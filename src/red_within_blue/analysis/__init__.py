"""Post-training analysis tools for RedWithinBlue experiments.

Modules
-------
threat_model
    Aggregate the adversarial Phase 1 sweeps into the threat-model
    degradation table, plots, and markdown report.
experiment_report
    Render a single training run's metrics + an episode GIF into a
    self-contained HTML report.
"""
